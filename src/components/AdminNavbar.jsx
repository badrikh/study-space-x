
import { Link, useLocation } from "react-router-dom"; import { ArrowLeft, BarChart3, CreditCard, LayoutGrid, Sparkles } from "lucide-react";
const items=[{label:"Dashboard",to:"/admin",icon:LayoutGrid},{label:"Orders",to:"/admin/orders",icon:CreditCard},{label:"Subscriptions",to:"/admin/subscriptions",icon:CreditCard},{label:"AI Analytics",to:"/admin/analytics",icon:BarChart3,pill:true}];
export default function AdminNavbar(){ const location=useLocation(); return (<header className="admin-topbar"><div className="container-fluid admin-shell px-4 px-xl-5"><div className="d-flex flex-wrap align-items-center justify-content-between gap-3 py-3"><div className="d-flex align-items-center gap-4"><Link className="admin-back-link text-decoration-none" to="/"><ArrowLeft size={20}/><span>Back to Site</span></Link><div className="admin-divider-line"/><div className="d-flex align-items-center gap-3"><div className="admin-brand-icon"><Sparkles size={22}/></div><h1 className="admin-panel-title mb-0">Admin Panel</h1></div></div><nav className="d-flex flex-wrap align-items-center gap-2 gap-lg-3">{items.map((item)=>{ const Icon=item.icon; const active=location.pathname===item.to; const cls=item.pill?`admin-analytics-pill ${active?"active":""}`:`admin-nav-link ${active?"active":""}`; return <Link key={item.to} to={item.to} className={cls}><Icon size={18}/><span>{item.label}</span></Link>})}</nav></div></div></header>) }
